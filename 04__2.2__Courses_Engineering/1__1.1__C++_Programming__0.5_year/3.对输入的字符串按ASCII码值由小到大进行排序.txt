#include<stdio.h>
#include<string.h>
void main()
{
	int i,j,k,m;
	char str[100];
	gets(str);
	k=strlen(str);
	for(j=k-1;j>0;j--)
	{
	for(i=0;i<j;i++)
	{
		if(str[i]>str[i+1])
		{
			m=str[i+1];
			str[i+1]=str[i];
			str[i]=m;
		}
	}
	}
	puts(str);
}
